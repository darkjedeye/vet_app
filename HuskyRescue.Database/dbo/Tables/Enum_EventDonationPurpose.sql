CREATE TABLE [dbo].[Enum_EventDonationPurpose] (
    [ID]    INT            NOT NULL,
    [Value] NVARCHAR (200) NOT NULL,
    CONSTRAINT [PK_Enum_EventDonationPurpose] PRIMARY KEY CLUSTERED ([ID] ASC)
);

