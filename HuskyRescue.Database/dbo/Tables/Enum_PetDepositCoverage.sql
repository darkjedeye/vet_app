CREATE TABLE [dbo].[Enum_PetDepositCoverage] (
    [ID]    INT           NOT NULL,
    [Value] NVARCHAR (50) NOT NULL,
    CONSTRAINT [PK_Enum_PetDepositCoverage] PRIMARY KEY CLUSTERED ([ID] ASC)
);

