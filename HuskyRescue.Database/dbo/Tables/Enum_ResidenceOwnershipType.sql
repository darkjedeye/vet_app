CREATE TABLE [dbo].[Enum_ResidenceOwnershipType] (
    [ID]    INT           NOT NULL,
    [Value] NVARCHAR (50) NOT NULL,
    CONSTRAINT [PK_Enum_ResidenceOwnershipType] PRIMARY KEY CLUSTERED ([ID] ASC)
);

